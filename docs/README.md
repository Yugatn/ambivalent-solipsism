# Публичная страница СТСети

Эта директория содержит статический сайт проекта СТСеть для GitHub Pages.

## Структура

- `index.html` — главная страница
- `assets/style.css` — оформление

Публикация предполагается через GitHub Pages из директории `docs/` ветки `main`.
