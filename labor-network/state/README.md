# State Layer

State описывает текущее состояние сущности, вычисленное из последовательности событий и действующих правил.

State не является единственным источником истории.

Изменение state должно быть трассируемо к событиям и policy.